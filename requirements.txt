# Vista Assistant — setup and test dependencies.
# The deployed app has its own, smaller list in app/requirements.txt.

databricks-sdk>=0.30.0
requests>=2.31.0

# document generation (the 8-file policy corpus)
reportlab>=4.0.0        # PDFs
python-pptx>=0.6.23     # the board pack
python-docx>=1.1.0      # the audit memo

# tests
pypdf>=4.0.0            # extracts text back out of the PDFs to verify their contents
vl-convert-python>=1.6  # renders Vega-Lite specs offline so chart tests can assert
playwright>=1.40.0      # browser-level UI checks (run: python -m playwright install chromium)
httpx>=0.27.0           # streaming assertions against a real server

# the architecture diagram
matplotlib>=3.7.0
